<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ACalc.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ACalc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ACalc">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Column="0" Grid.Row="0">
            <Image Source="/Assets/Square44x44Logo.png" Width="20" HorizontalAlignment="Left" Margin="10,10,10,0"/>
        </Grid>

        <Grid Grid.Column="0" Grid.Row="1" Margin="10,10,10,10" CornerRadius="10" Background="#22000000">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:Name="DisplayGrid" Grid.Column="0" Grid.Row="0" Background="#AA000000">

            </Grid>

            <Grid Grid.Column="0" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Column="0" Grid.Row="0" Background="#44000000">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Button x:Name="AddNewSubButton" Grid.Column="1" Grid.Row="0" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Margin="10,10,10,10" Click="AddNewSubButton_Click">
                        <Button.Content>
                            <FontIcon Glyph="&#xE710;" FontSize="10"/>
                        </Button.Content>
                    </Button>
                </Grid>

                <Grid x:Name="SubjectListGrid" Grid.Column="0" Grid.Row="1">
                    <ListView x:Name="Subjects_ListView" Loaded="Subjects_ListView_Loaded">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:Subjects">
                                <Grid Padding="5,5,5,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{x:Bind SubjectName}" Margin="10,10,10,10" VerticalAlignment="Center"/>
                                    <NumberBox Grid.Column="1" Value="{x:Bind ClassesAttended}" Margin="10,10,10,10"/>
                                    <NumberBox Grid.Column="2" Value="{x:Bind ClassesConducted}" Margin="10,10,10,10"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>

            </Grid>
        </Grid>
        
    </Grid>
</Window>
